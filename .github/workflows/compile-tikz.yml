name: Compile TikZ Assets

on:
  push:
    paths:
      - "src/**"
      - ".github/workflows/compile-tikz.yml"
  pull_request:
    paths:
      - "src/**"
      - ".github/workflows/compile-tikz.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install LaTeX dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            latexmk \
            pdf2svg \
            texlive-xetex \
            texlive-lang-chinese \
            texlive-latex-extra

      - name: Compile TeX files to PDF and SVG
        env:
          CTEX_FONTSET: fandol
          OUTPUT_DIR: output
        run: |
          ./scripts/build_assets.sh

      - name: Upload PDF and SVG artifacts
        uses: actions/upload-artifact@v4
        with:
          name: compiled-assets
          path: |
            output/*.pdf
            output/*.svg
          if-no-files-found: error

      - name: Commit generated PDF and SVG files
        if: github.event_name == 'push'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "ci: update compiled assets"
          file_pattern: "output/*.pdf output/*.svg"
